<?xml version="1.0" encoding="utf-8"?>
<odoo>

<record id="view_event_registration_form_inherit" model="ir.ui.view">
    <field name="name">event.registration.form.inherit.custom</field>
    <field name="model">event.registration</field>
    <field name="inherit_id" ref="event.view_event_registration_form"/>
    <field name="arch" type="xml">
        <!-- Replace the existing "Registered" button with a more descriptive "Approve Registration" -->
        <xpath expr="//header/button[@name='action_confirm']" position="attributes">
            <attribute name="string">Approve Registration</attribute>
            <attribute name="class">oe_highlight btn-success</attribute>
        </xpath>
    </field>
</record>

</odoo>
