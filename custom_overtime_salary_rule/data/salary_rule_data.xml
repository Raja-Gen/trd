<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <!-- Salary Rule: Overtime Hours-->
        <record id="salary_rule_overtime_hours" model="hr.salary.rule">
            <field name="name">Overtime Hours</field>
            <field name="code">OH</field>
            <field name="sequence">5</field>
            <field name="category_id" ref="hr_payroll.ALW"/> <!-- Refers to the Deduction category -->
            <field name="condition_select">python</field>
            <field name="condition_python">
total_overtime_hours = 0
if payslip.worked_days_line_ids:
    for line in payslip.worked_days_line_ids[0]:
        total_overtime_hours += line.total_overtime_hours
result = total_overtime_hours
            </field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute">
total_overtime_hours = 0
if payslip.worked_days_line_ids:
    for line in payslip.worked_days_line_ids[0]:
        total_overtime_hours += line.total_overtime_hours
result = total_overtime_hours
            </field>
        <field name="struct_id" ref="kuwait_payroll.kuwait_employee_payroll_structure"/>
        </record>
    </data>
</odoo>
