<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="crm_stage_form_quotation_flag" model="ir.ui.view">
        <field name="name">crm.stage.form.quotation.flag</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_stage_form"/>
        <field name="arch" type="xml">
            <field name="is_won" position="before">
                <field name="is_quotation_stage" invisible="is_won"/>
            </field>
        </field>
    </record>

</odoo>
